---
interface Props {
  id: string,
  backGroundColor: string;
}

const { id, backGroundColor } = Astro.props;
---

<section id={id} class=`w-full mx-auto px-5 py-10 md:px-24 ${backGroundColor}`>
  <slot />
</section>
