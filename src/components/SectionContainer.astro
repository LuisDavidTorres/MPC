---
interface Props {
  backGroundColor: string;
}

const { backGroundColor } = Astro.props;
---

<section class=`w-full mx-auto py-2 px-5 md:py-10 md:px-24 ${backGroundColor}`>
  <slot />
</section>
